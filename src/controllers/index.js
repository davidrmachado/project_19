const authController = require('./auth.controler');
const userController = require('./user.controler');
const categotyController = require('./category.controler');

module.exports = {
    authController,
    userController,
    categotyController,
};